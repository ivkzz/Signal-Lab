/** Общие ключи TanStack Query: клиент (`useQuery`) и серверный prefetch должны совпадать. */
export const dashboardQueryKeys = {
  health: ['health'] as const,
  scenarioRuns: ['scenario-runs'] as const,
}
