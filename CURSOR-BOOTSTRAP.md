# CURSOR BOOTSTRAP — Автоматическая настройка AI-слоя проекта

Ты — главный эксперт по Cursor IDE.  
Твоя задача: полностью изучить этот проект и настроить/улучшить идеальный AI-слой.

**Важное правило:**
Если уже существуют папки `.cursor/hooks/`, `.cursor/skills/`, `.cursor/commands/`, `.cursor/rules/` или файл `CURSOR_AI_LAYER.md` — не удаляй и не перезаписывай их полностью. Проанализируй содержимое, улучшай и дополняй, сохраняя всё полезное.

Выполняй строго по шагам. После каждого крупного шага кратко отчитывайся.

### 0. Token Economy & Model Selection Strategy (ОБЯЗАТЕЛЬНО СНАЧАЛА)
Создай/улучши файл `.cursor/rules/token-economy.mdc` со следующим содержимым:

**Правила экономии токенов и выбора моделей (15 апреля 2026)**

- По умолчанию всегда работай в **Auto** mode.
- Для 80–90% задач (обычная разработка, правки, небольшие рефакторинги) используй быстрые модели: **Composer 2**, **Kimi K2.5 **, ***Gemini 3 Flash **, Claude 4.6 Sonnet** или **GPT-5.3 Codex**.
- Для сложных задач (архитектура, сложная логика) сначала сделай полный план на быстрой модели, затем явно напиши в ответе: «Для этой задачи рекомендую переключить модель на Claude 4.6 Opus» и продолжай работу.
- Строго экономь токены:
  - Используй только точечный контекст (@файлы, @папки, @codebase).
  - Предпочитай Skills и Hooks вместо длинных объяснений в чате.
  - Делай короткие сессии (новый чат на новую задачу).
  - Никогда не повторяй правила — ссылайся на `@.cursor/rules/`.
  - Следи за `preCompact` hook — если контекст >70%, автоматически сокращай.

Эти правила применяются **всегда** в каждом твоём ответе.

### 1. Полный анализ проекта
Изучи структуру, package.json, стек и проблемы. Дай краткий отчёт.

### 2. Rules
Создай/улучши папку `.cursor/rules/`:
- `core.mdc`
- `architecture.mdc`
- `stack-specific.mdc`
- `token-economy.mdc` (из шага 0)

### 3. Hooks
Создай/улучши `.cursor/hooks.json` и папку `.cursor/hooks/`.

### 4. Skills
Создай/улучши папку `.cursor/skills/` (каждый skill — отдельная папка + SKILL.md).

### 5. Commands
Создай/улучши папку `.cursor/commands/`.

### 6. CURSOR_AI_LAYER.md
Создай/улучши файл `CURSOR_AI_LAYER.md` в корне проекта.

### 7. Финализация
1. Покажи полную структуру папки `.cursor/`
2. Предложи протестировать один hook и один skill
3. Подтверди, что все правила (включая token economy) применяются

Начинай прямо сейчас с шага 0.